package day17;

public class BoxSwap {

	/*
	public static void swapbox(Box<? extends Number> a, Box<? extends Number> b) {
		Box<Number> tmp = new Box<>();
		tmp.set(a.get());
		a.set(b.get());
		b.set(tmp.get());

	}
	
	public static void swapbox2(Box<?> a, Box<?> b) {
		Box<?> tmp = new Box<>();
		/*
		tmp = a;
		a = b;
		b = tmp;
		*/
	/*
		tmp.set(a.get());
	    a.set(b.get());
	    b.set(tmp);
	}*/
}
